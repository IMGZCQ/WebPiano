name: Publish and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      build_type:
        description: '选择构建类型'
        required: true
        default: 'docker_and_release'
        type: choice
        options:
          - docker_and_release
          - docker
          - release
      tag_name:
        description: '输入版本号(0.2.2)'
        required: true
        default: '0.2.2'
      push_latest:
        description: '系咪同时推送latest标签?'
        required: false
        default: true
        type: boolean

env:
  DOCKERHUB_REGISTRY: docker.io
  DOCKERHUB_IMAGE_NAME: imgzcq/WebPiano
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  docker:
    name: Build and Push Docker
    if: >-
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' && (inputs.build_type == 'docker' || inputs.build_type == 'docker_and_release'))
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.1'
          cache: false

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Verify Docker Hub credentials
        id: check_dockerhub
        run: |
          if [ -z "$DH_USER" ] || [ -z "$DH_TOKEN" ]; then
            echo "::warning::DOCKERHUB_USERNAME or DOCKERHUB_TOKEN secret is not set. Docker push will be skipped. Add them in Settings → Secrets and variables → Actions to enable."
            echo "has_creds=false" >> "$GITHUB_OUTPUT"
          else
            echo "has_creds=true" >> "$GITHUB_OUTPUT"
          fi
        env:
          DH_USER: ${{ secrets.DOCKERHUB_USERNAME }}
          DH_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Log into Docker Hub
        if: steps.check_dockerhub.outputs.has_creds == 'true'
        uses: docker/login-action@v4
        with:
          registry: ${{ env.DOCKERHUB_REGISTRY }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push to Docker Hub
        if: steps.check_dockerhub.outputs.has_creds == 'true'
        uses: docker/build-push-action@v7
        with:
          context: .
          push: true
          tags: |
            ${{ env.DOCKERHUB_REGISTRY }}/${{ env.DOCKERHUB_IMAGE_NAME }}:${{ inputs.tag_name || github.ref_name }}
            ${{ inputs.push_latest && format('{0}/{1}:latest', env.DOCKERHUB_REGISTRY, env.DOCKERHUB_IMAGE_NAME) || '' }}
          platforms: linux/amd64,linux/arm64,linux/arm/v7
          cache-from: type=gha
          cache-to: type=gha,mode=max

  release:
    name: Build Binaries and Create Release
    # 与 docker job 解耦：docker 推送需要 Docker Hub 凭证，二进制发布只要 GITHUB_TOKEN
    if: >-
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' && (inputs.build_type == 'release' || inputs.build_type == 'docker_and_release'))
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.26.1'
          cache: false

      - name: Determine Release Tag
        id: get_tag
        run: |
          if [[ "${{ github.event_name }}" == "push" ]]; then
            RELEASE_TAG="${{ github.ref_name }}"
          else
            INPUT_TAG="${{ inputs.tag_name }}"
            if [[ "$INPUT_TAG" == v* ]]; then
              RELEASE_TAG="$INPUT_TAG"
            else
              RELEASE_TAG="v$INPUT_TAG"
            fi
          fi
          echo "release_tag=$RELEASE_TAG" >> $GITHUB_OUTPUT

      - name: Build binaries
        run: |
          VERSION=${{ steps.get_tag.outputs.release_tag }}

          GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o go-piano-linux-amd64 main.go
          GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o go-piano-linux-arm64 main.go
          GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o go-piano-windows-amd64.exe main.go
          GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o go-piano-darwin-amd64 main.go
          GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o go-piano-darwin-arm64 main.go

      - name: Generate checksums
        run: |
          sha256sum go-piano-linux-amd64 > go-piano-linux-amd64.sha256
          sha256sum go-piano-linux-arm64 > go-piano-linux-arm64.sha256
          sha256sum go-piano-windows-amd64.exe > go-piano-windows-amd64.exe.sha256
          sha256sum go-piano-darwin-amd64 > go-piano-darwin-amd64.sha256
          sha256sum go-piano-darwin-arm64 > go-piano-darwin-arm64.sha256

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.get_tag.outputs.release_tag }}
          name: Release ${{ steps.get_tag.outputs.release_tag }}
          files: |
            go-piano-linux-amd64
            go-piano-linux-amd64.sha256
            go-piano-linux-arm64
            go-piano-linux-arm64.sha256
            go-piano-windows-amd64.exe
            go-piano-windows-amd64.exe.sha256
            go-piano-darwin-amd64
            go-piano-darwin-amd64.sha256
            go-piano-darwin-arm64
            go-piano-darwin-arm64.sha256
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
